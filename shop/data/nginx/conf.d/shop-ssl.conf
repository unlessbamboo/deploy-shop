server {
    listen       443 ssl http2 default_server;
    server_name          unusebamboo.top;
    root  /data/bamboo;

    ssl_certificate "/data/ssl/unusebamboo.top.pem";
    ssl_certificate_key "/data/ssl/unusebamboo.top.key";
    ssl_session_cache shared:SSL:1m;
    ssl_session_timeout  10m;
    ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
    ssl_prefer_server_ciphers on;

    server_tokens        off;
    large_client_header_buffers 4 16k;
    add_header Cache-Control no-cache;
    add_header Cache-Control private;
    if ($request_method !~ ^(GET|HEAD|POST|PUT|PATCH|DELETE|OPTIONS)$ ) {
	    return           404;
	}
}

server {
    listen       443 ssl http2;
    server_name          shop.unusebamboo.top;
    root  /data/shop;

    ssl_certificate "/data/ssl/shop.unusebamboo.top.pem";
    ssl_certificate_key "/data/ssl/shop.unusebamboo.top.key";
    ssl_session_cache shared:SSL:1m;
    ssl_session_timeout  10m;
    ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
    ssl_prefer_server_ciphers on;

    server_tokens        off;
    large_client_header_buffers 4 16k;
    add_header Cache-Control no-cache;
    add_header Cache-Control private;
    if ($request_method !~ ^(GET|HEAD|POST|PUT|PATCH|DELETE|OPTIONS)$ ) {
	    return           404;
	}
}

server {
    listen       443 ssl http2;
    server_name          flaskapi.unusebamboo.top;
    root  /data/shop;

    ssl_certificate "/data/ssl/flaskapi.unusebamboo.top.pem";
    ssl_certificate_key "/data/ssl/flaskapi.unusebamboo.top.key";
    ssl_session_cache shared:SSL:1m;
    ssl_session_timeout  10m;
    ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
    ssl_prefer_server_ciphers on;

    server_tokens        off;
    large_client_header_buffers 4 16k;
    add_header Cache-Control no-cache;
    add_header Cache-Control private;
    if ($request_method !~ ^(GET|HEAD|POST|PUT|PATCH|DELETE|OPTIONS)$ ) {
	    return           404;
	}

    location /  {
      #deny all;
      add_header               Strict-Transport-Security "max-age=31536000; includeSubDomains; preload";
      #add_header              X-Frame-Options deny;
      proxy_set_header         Host $proxy_host;
      proxy_set_header         X-Real_IP        $remote_addr;
      proxy_set_header         X-Forwarded-For  $proxy_add_x_forwarded_for;
      proxy_pass               http://shop-api:8010;
      proxy_redirect           off;
      proxy_connect_timeout    120s;
      proxy_read_timeout       300s;
      proxy_intercept_errors   on;
      send_timeout             2000s;
      fastcgi_buffers          8 128k;
      client_max_body_size     10m;
    }
}


server {
    listen       443 ssl http2;
    server_name          ginapi.unusebamboo.top;
    root  /data/shop;

    ssl_certificate "/data/ssl/ginapi.unusebamboo.top.pem";
    ssl_certificate_key "/data/ssl/ginapi.unusebamboo.top.key";
    ssl_session_cache shared:SSL:1m;
    ssl_session_timeout  10m;
    ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
    ssl_prefer_server_ciphers on;

    server_tokens        off;
    large_client_header_buffers 4 16k;
    add_header Cache-Control no-cache;
    add_header Cache-Control private;
    if ($request_method !~ ^(GET|HEAD|POST|PUT|PATCH|DELETE|OPTIONS)$ ) {
	    return           404;
	}

    location /  {
      #deny all;
      add_header               Strict-Transport-Security "max-age=31536000; includeSubDomains; preload";
      #add_header              X-Frame-Options deny;
      proxy_set_header         Host $proxy_host;
      proxy_set_header         X-Real_IP        $remote_addr;
      proxy_set_header         X-Forwarded-For  $proxy_add_x_forwarded_for;
      proxy_pass               http://shop-api:8010;
      proxy_redirect           off;
      proxy_connect_timeout    120s;
      proxy_read_timeout       300s;
      proxy_intercept_errors   on;
      send_timeout             2000s;
      fastcgi_buffers          8 128k;
      client_max_body_size     10m;
    }
}
